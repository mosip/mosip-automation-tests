{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
    "schema": [	
    {{#each schema}}
      {
        "id": "{{id}}",
        "inputRequired": "{{inputRequired}}",
        "required": "{{required}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}