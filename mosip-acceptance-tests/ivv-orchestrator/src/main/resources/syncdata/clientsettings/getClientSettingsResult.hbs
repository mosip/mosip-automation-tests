{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
      "lastSyncTime": "$IGNORE$",
     "dataToSync": ["$IGNORE$"],
    "dataToSync1": [	
    {{#each dataToSync}}
      {
        "entityName": "{{entityName}}",
        "entityType": "{{entityType}}",
        "data": "{{data}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}