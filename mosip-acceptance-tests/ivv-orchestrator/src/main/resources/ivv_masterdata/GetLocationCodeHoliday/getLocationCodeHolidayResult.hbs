{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response":{
  "holidays": [	
    {{#each response}}
      {
        "locationCode": "$IGNORE$",
        "holidayDate": "$IGNORE$",
        "holidayName": "$IGNORE$",
        "holidayDesc": "$IGNORE$",
        "langCode": "$IGNORE$",
        "isActive": "$IGNORE$",
        "holidayId": "$IGNORE$"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]}
  ,
  "errors": "$IGNORE$"
}