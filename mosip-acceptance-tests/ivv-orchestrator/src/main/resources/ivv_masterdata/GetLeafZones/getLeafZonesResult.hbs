{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": [	
    {{#each response}}
      {
        "code": "{{code}}",
        "langCode": "{{langCode}}",
        "name": "{{name}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  ,
  "errors": "$IGNORE$"
}