{
  "request": {
   "transactionId": "{{transactionId}}",
   "encodedHash": "{{encodedHash}}",
    "name": "{{name}}",
    "policyId": "{{policyId}}",
    "publicKey": {{publicKey}},
    "authPartnerId": "{{authPartnerId}}",
    "logoUri": "{{logoUri}}",
    "redirectUris": [
          "{{redirectUris}}"
  ],
    "grantTypes": [
            "{{grantTypes}}"
        ],
    "clientAuthMethods": [
            "{{clientAuthMethods}}"
        ]
  }
}